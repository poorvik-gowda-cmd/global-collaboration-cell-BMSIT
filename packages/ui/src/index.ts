/**
 * @gcc-portal/ui
 *
 * Shared React component library for GCC Portal.
 * Components are exported from this single entry point.
 *
 * This package is in early development — only foundation components exist.
 * A full design system will be built as the GCC Portal UI is designed.
 */

export { Button } from "./components/Button.js";
export type { ButtonProps } from "./components/Button.js";
