{
  "name": "@gcc-portal/auth",
  "version": "0.0.1",
  "private": true,
  "description": "Authentication interfaces and helpers for GCC Portal (Google OAuth/OIDC — implementation pending)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@gcc-portal/contracts": "workspace:*"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  }
}
